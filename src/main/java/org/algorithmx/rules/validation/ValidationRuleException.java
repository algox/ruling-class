package org.algorithmx.rules.validation;

import org.algorithmx.rules.core.UnrulyException;

public class ValidationRuleException extends UnrulyException {

    public ValidationRuleException(String message) {
        super(message);
    }
}
