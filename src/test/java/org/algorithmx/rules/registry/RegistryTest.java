package org.algorithmx.rules.registry;

import org.junit.Test;

import java.math.BigDecimal;

public class RegistryTest {

    public RegistryTest() {
        super();
    }

    @Test
    public void test1() {
        /*RuleSet x = null;

        x.register("test", (String y) -> y.equals(""),
                "")
                .register("test rule 2", (String a, BigDecimal b) -> b != null,
                        "This test is to make sure its working!")
                .register("", (String a, String b, Integer c) -> true, "")
                .register("", () -> true, "");*/

    }
}
